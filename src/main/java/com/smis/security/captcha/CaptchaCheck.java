
package com.smis.security.captcha;

import java.util.*;
import java.io.*;

public class CaptchaCheck {

	/*
	 * // Returns true if given two strings are same public static boolean
	 * checkCaptcha(String captcha, String user_captcha) { return
	 * captcha.equals(user_captcha); }
	 * 
	 * // Generates a CAPTCHA of given length public static String
	 * generateCaptcha(int n) { // to generate random integers in the range [0-61]
	 * Random rand = new Random(62);
	 * 
	 * // Characters to be included //String chrs =
	 * "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"; //String
	 * chrs = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"; String chrs = "0123456789"; //
	 * Generate n characters from above set and // add these characters to captcha.
	 * String captcha = ""; while (n-- > 0) { //int index = (int) (Math.random() *
	 * 62); //int index = (int) (Math.random() * 36); int index = (int)
	 * (Math.random() * 10); captcha += chrs.charAt(index); }
	 * 
	 * return captcha; }
	 * 
	 * // Driver code
	 */	
}